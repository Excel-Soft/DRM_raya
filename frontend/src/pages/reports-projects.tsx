// The Reports → Projects route renders the same real, DB-backed Project Report
// screen as /pms/project-report. Re-exported to avoid two inconsistent screens.
export { default } from "./pms-project-report";
