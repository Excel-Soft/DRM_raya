# PATCH 2 — Issue Matrix

> Baseline (Stage 0). **Status = current state, not yet fixed.** No code changed
> in this stage. Priority: P1 (broken / fake / mock visible to users), P2
> (gaps / inconsistency), P3 (clarification / polish).

| Issue ID | Priority | Module | Route | Frontend file | Backend route | DB table / source | Current problem | Required fix (later stage) | Stage | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| PEN-001 | P3 | Penalty | `/drm/add-penalty` | `client/src/pages/drm/add-penalty.tsx` | `POST/PATCH /api/penalties` (`server/penalty-routes.ts`) | `drm.penalties` | Reported "not saving" — **not reproduced**; saves via real mutation | Verify against reporter's exact steps; likely a data/permission case | TBD | NOT REPRODUCED |
| PEN-002 | P3 | Penalty | Service Mgr > Add Penalty | `client/src/pages/service-add-penalty.tsx` | `POST /api/penalties` | `drm.penalties` | Reported "no endpoint" — **endpoint exists & registered** | None needed; confirm routing from service dashboard | TBD | NOT REPRODUCED |
| PEN-003 | P3 | Penalty | `/drm/add-penalty` | `add-penalty.tsx`, `service-add-penalty.tsx` | `/api/penalties` | `drm.penalties` | Reported fake toast — **toasts are real (onSuccess after 2xx)** | None needed | TBD | NOT REPRODUCED |
| RPT-RAW-001 | P2 | Reports/Accounts | `/reports/raw-attendance` | `client/src/pages/reports-raw-attendance.tsx` | `GET /api/attendance/raw` (`attendance-edit-routes.ts`) | None (no source) | No data source; returns honest empty `records: []` | Wire a real raw-attendance source | TBD | OPEN (no source) |
| RPT-RAW-002 | P2 | Reports/Accounts | `/reports/raw-attendance` | `reports-raw-attendance.tsx` | `GET /api/attendance/raw` | None | Empty/loading/error states minimal | Standardize states once source exists | TBD | OPEN |
| SAL-CREATE-001 | P2 | Reports/Accounts | `/reports/salary-create` | `client/src/pages/salary-create.tsx` | `GET /api/salary/preview`, `POST /api/salary/runs` | `drm.salary_runs`, `salary_run_items`, `users`, `attendance` | Overtime & Other Deductions hardcoded `0` | Source OT/deductions instead of constant 0 | TBD | OPEN (partial source) |
| SAL-CREATE-002 | P3 | Reports/Accounts | `/reports/salary-create` | `salary-create.tsx` | `POST /api/salary/runs` | `salary_runs` | Persists correctly; gated to SALARY_ROLES | None needed; confirm rounding/edge cases | TBD | OK |
| SAL-REPORT-001 | P3 | Reports/Accounts | `/reports/salary` | `client/src/pages/salary-report.tsx` | `GET /api/salary/runs`, `/:id` | `salary_runs`, `salary_run_items` | Reads real runs; print via `window.print()` | None needed; confirm slip layout | TBD | OK |
| EVT-001 | P2 | Reports/Accounts | `/reports/event` | `client/src/pages/reports-event.tsx` | `GET /api/events/report` (`events-routes.ts`) | `drm.events`, `event_speakers` | GET report requires auth only (no role gate); CSV is client-side | Add action-level gating if required | TBD | OPEN (perm) |
| REC-001 | P3 | Reports/Accounts | `/reports/reception` | `client/src/pages/reports-reception.tsx` | `GET /api/reports/reception` (`stage3-reports-routes.ts`) | `drm.meetings ⨝ customers` | Auth-only access; functional | Confirm role gating expectation | TBD | OK |
| EDIT-ATT-001 | P3 | Reports/Accounts | `/reports/edit-att` | `client/src/pages/reports-edit-att.tsx` | `GET /api/attendance/edits`, `PATCH .../:id/approve\|reject` | `drm.attendance_edit_requests` | Functional; approve/reject gated to managerial | None needed | TBD | OK |
| DAY-TARGET-001 | P1 | Reports/Accounts | `/reports/day-target` | `client/src/pages/reports-day-target.tsx` | `GET /api/reports/day-target` → catch-all 400 "Invalid report type" | None | Endpoint unsupported (not in `[loan,vas,gm,bv]`) | Add real day-target endpoint + source/table | TBD | OPEN |
| DAY-TARGET-002 | P1 | Reports/Accounts | `/reports/day-target` | `reports-day-target.tsx` (≈L53) | — | None | **Hardcoded mock rows** ("Shaila Khaild") on failure | Remove mock fallback; honest empty/error state | TBD | OPEN |
| DIAG-001 | P3 | Reports/Accounts | `/reports/diagnose` | `client/src/pages/reports-diagnose.tsx` | `GET /api/reports/bv` (catch-all → `getBvReport`) | `bv` source | Endpoint exists; screen is a thin BV viewer | Clarify scope vs BV group; align naming | TBD | OPEN (clarify) |
| DIAG-002 | P3 | Reports/Accounts | `/reports/diagnose` | `reports-diagnose.tsx` | `GET /api/reports/bv` | `bv` source | Honest "No entries" on null (no mock) | None needed beyond scope clarification | TBD | OK |
| BV-001 | P2 | User Reports / BV | `/reports/bv` | `client/src/pages/user-reports.tsx` | `GET /api/reports/bv`, `/api/bv-reports` | `drm.bv_reports` + `drm.bv_entries` | BV data fragmented across two tables | Decide authoritative table; document | TBD | OPEN |
| BV-002 | P2 | User Reports / BV | Pending BV | `client/src/pages/reports-bv-pending-rc.tsx` | none (`/api/reports/pending-bv` missing) | None | Honest empty static; no backend source | Add source or formally retire page | TBD | OPEN (no source) |
| BV-003 | P1 | User Reports / BV | BV export | `client/src/pages/user-reports.tsx` (≈L1000) | `GET /api/reports/:reportType/export-csv` (no `bv` case) | `bv` source | Server CSV export lacks `bv`; client export works | Add `bv` case to server export for parity | TBD | OPEN |
| BV-004 | P1 | User Reports / BV | Email report | `user-reports.tsx` | email route `reports-routes.ts` (≈L1023) | — | Returns `success: true` ("Email integration pending") — **fake success** | Fail honestly until email integration lands | TBD | OPEN |
| GLOBAL-001 | P1 | Global | all reports | `app-sidebar.tsx`, `App.tsx`, report pages | `/api/reports/*` | `drm.menu_permissions` | Action-level (view/create/edit/export/finalize) perms inconsistent; some GETs auth-only | Standardize action-level permission checks | TBD | OPEN |
| GLOBAL-002 | P1 | Global | all reports | report `*.tsx` pages | `/api/reports/*` | various | Mock fallback (day-target), one fake success (email), inconsistent loading/empty/error states | Remove mocks; honest states; standardize per `add-penalty.tsx` | TBD | OPEN |
