diff --cc client/src/pages/account-manager-dashboard.tsx
index b8a5a82,8519c0d..0000000
--- a/client/src/pages/account-manager-dashboard.tsx
+++ b/client/src/pages/account-manager-dashboard.tsx
@@@ -1,5 -1,5 +1,10 @@@
++<<<<<<< HEAD
 +import { useQuery, useMutation } from "@tanstack/react-query";
 +import { apiRequest, queryClient } from "@/lib/queryClient";
++=======
+ import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
+ import { apiRequest } from "@/lib/queryClient";
++>>>>>>> bilal
  import { useState } from "react";
  import { Link, useLocation } from "wouter";
  import { GmApprovalCard } from "@/components/gm-approval-card";
@@@ -24,11 -25,10 +30,12 @@@ import 
      PieChart,
      ArrowRight,
      DollarSign,
 +    Check,
 +    X as XIcon,
      Calendar,
      Eye,
-     X
+     X,
+     Edit
  } from "lucide-react";
  import {
      Select,
diff --cc client/src/pages/lead-pools.tsx
index 4ee83ba,dbc94f6..0000000
--- a/client/src/pages/lead-pools.tsx
+++ b/client/src/pages/lead-pools.tsx
@@@ -2240,17 -2248,14 +2252,28 @@@ export default function LeadPools() 
                          </Button>
                          <Button
                            size="sm"
++<<<<<<< HEAD
 +                          variant="outline"
 +                          className="bg-slate-100 text-slate-800 border-slate-300 gap-2"
 +                          onClick={() => {
 +                            if (viewLead) {
 +                              setShowInvoiceForm(true);
 +                            } else {
 +                              toast({ title: "No lead selected", variant: "destructive" });
 +                            }
 +                          }}
 +                        >
 +                          <FileText className="h-4 w-4" />
++=======
+                           className="bg-indigo-500 hover:bg-indigo-600 text-white gap-2"
+                           onClick={() => {
+                             if (viewLead) {
+                               setLocation(`/sales/create-invoice/${viewLead.id}`);
+                             }
+                           }}
+                         >
+                           <FileText className="w-4 h-4" />
++>>>>>>> bilal
                            Invoice
                          </Button>
                          <Button
diff --cc server/repositories/hod.repository.ts
index 664bc44,f99eb91..0000000
--- a/server/repositories/hod.repository.ts
+++ b/server/repositories/hod.repository.ts
@@@ -69,32 -69,32 +69,58 @@@ const pendingApprovalsUnion = 
             g.id::text as id,
             g.id::text as "referenceId",
             g.created_by::text as "submittedById",
-            g.created_at as "date",
-            COALESCE(g.approval_status, 'pending_hod') as status,
+            g.created_at as "createdAt",
+            COALESCE(g.approval_status, 'Pending')::text as status,
             'gm_entries'::text as source,
++<<<<<<< HEAD
 +           c.company_name as "companyName",
 +           c.drm_id as "drmId",
 +           g.amount_usd as "orderDollar",
 +           g.member_id as "memberId",
 +           g.package_type as "packageName"
 +    from drm.gm_entries g
 +    left join drm.customers c on c.id = g.customer_id
 +    where g.approval_status IS NULL OR g.approval_status = 'pending_hod'
 +    union all
 +    select 'Invoice'::text as type,
 +           q.id::text as id,
 +           q.id::text as "referenceId",
 +           q.created_by::text as "submittedById",
 +           q.created_at as "date",
 +           case when q.save_status = 'pending_hod' then 'Pending' else q.save_status end as status,
 +           'quotations'::text as source,
 +           q.company as "companyName",
 +           NULL::text as "drmId",
 +           q.grand_total as "orderDollar",
 +           NULL::text as "memberId",
 +           NULL::text as "packageName"
 +    from drm.quotations q
 +    where q.save_status = 'pending_hod'
++=======
+            c.company_name::text as "companyName",
+            c.drm_id::text as "drmId",
+            g.amount_usd::numeric as "orderDollar",
+            g.member_id::text as "memberId",
+            g.package_type::text as "packageName"
+     from drm.gm_entries g
+     left join drm.customers c on c.id = g.customer_id
+     where g.approval_status IS NULL OR g.approval_status = 'Pending'
+     union all
+     select 'Invoice'::text as type,
+            id::text as id,
+            id::text as "referenceId",
+            created_by_user_id::text as "submittedById",
+            created_at as "createdAt",
+            status::text as status,
+            'invoices'::text as source,
+            customer_name::text as "companyName",
+            NULL::text as "drmId",
+            total::numeric as "orderDollar",
+            invoice_number::text as "memberId",
+            NULL::text as "packageName"
+     from drm.invoices
+     where status::text = 'Pending'
++>>>>>>> bilal
    )
    select p.*,
           u.full_name as "submittedByName"
@@@ -209,11 -212,11 +238,19 @@@ export class HodRepository 
            [id, approverId],
          );
          break;
++<<<<<<< HEAD
 +      case "quotations":
 +        await pool.query(
 +          `update drm.quotations 
 +           set save_status = 'pending_account_manager', updated_at = now() 
 +           where id = $1 and save_status = 'pending_hod'`,
++=======
+       case "invoices":
+         await pool.query(
+           `update drm.invoices
+            set status = 'Sent', updated_at = now()
+            where id = $1 and status = 'Pending'`,
++>>>>>>> bilal
            [id],
          );
          break;
@@@ -269,12 -273,12 +307,21 @@@
            [id, approverId, reason ?? null],
          );
          break;
++<<<<<<< HEAD
 +      case "quotations":
 +        await pool.query(
 +          `update drm.quotations 
 +           set save_status = 'Rejected', updated_at = now(), note = coalesce($2, note)
 +           where id = $1 and save_status = 'pending_hod'`,
 +          [id, reason ?? null],
++=======
+       case "invoices":
+         await pool.query(
+           `update drm.invoices
+            set status = 'Cancelled', notes = coalesce(notes, '') || ' | Rejected by HOD: ' || $2::text, updated_at = now()
+            where id = $1 and status = 'Pending'`,
+           [id, reason ?? ""],
++>>>>>>> bilal
          );
          break;
        default:
