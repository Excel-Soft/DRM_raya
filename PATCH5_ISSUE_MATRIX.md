# PATCH 5 — Issue Matrix (Stage 0 baseline)

Status legend: **Open** = gap confirmed in code, not started (this is a baseline
audit; no fixes were made). All findings are code-verified per
`PATCH5_BASELINE_AUDIT.md`. "Mgmt confirm" = a business rule must be confirmed
before implementation (see `PATCH5_MANAGEMENT_CONFIRMATION_REQUIRED.md`).

| Patch ID | Priority | Requirement | Current backend files | Current frontend files | Current schema / table source | Current gap | Required fix | Mgmt confirm | Stage | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| **P1** | P0 | Restrict GM creation to approved initiating roles | `gm-pool-routes.ts:285` (`POST /api/gm`); `account-routes.ts:388` (`/gm-entries`); `settings.middleware.ts:14,102-110` (`checkUrlPermission`); `utils/role-utils.ts` | `gm-pool-add-gm.tsx`; `account-gm-entries.tsx`; `app-sidebar.tsx` (exposes to ~20 roles) | `drm.url_permissions` (unpopulated); `gm_entries` | URL-permission middleware **fails open** on no match + table empty ⇒ GM create is **auth-only, no role allow-list**. Frontend gating only. | Add explicit backend role allow-list (initiating roles) on all GM-create routes; do not rely on fail-open URL check or button hiding | **Yes** (Q1) | 2 | Open |
| **P2** | P0 | Explicit Full / Partial / Loan GM workflow routing | `gm-pool-routes.ts:305-306`; `account-routes.ts:388` | `gm-pool-add-gm.tsx` (`loanMode`), `account-gm-entries.tsx` | `gm_entries.is_loan`, `is_partial_payment` (int flags), `installments` jsonb; `gm_entry_type` enum = GM/TempGM/RefundGM only | Full/Partial/Loan are **implicit boolean flags**, not an enum; no controlled state transitions per type | Introduce `Full|Partial|Loan` enum + controlled routing/state machine; migrate flags | No | 1 | Open |
| **P3** | P0 | Minimum payment threshold validation | `gm-pool-routes.ts` (createSchema, L296); `account-routes.ts:397` | `gm-pool-add-gm.tsx`, `account-gm-entries.tsx` | `gm_entries` amount columns | Only `>0` checks; **no minimum threshold** by GM type/package | Add server-side min-threshold validation per type/package with clear UI messages | **Yes** (Q3) | 4 | Open |
| **P4** | P0 | Partial payment receipt collection + final approval workflow | `gm-pool-routes.ts` (approval ~L676) | `account-gm-entries.tsx`, `account-manager-dashboard.tsx` | `gm_entries.installments` jsonb, `is_partial_payment`, `payment_proof_url` | Receipts live in JSON blob; no first-class receipt rows; **no final-approval gate requiring full payment**; balance implicit | First-class receipt entries + remaining-balance tracking + approval gate; dashboard display | No | 6 | Open |
| **P5** | P0 | Loan GM admin approval + return-date tracking | `loan-routes.ts:214,258,374`; `reports-routes.ts:642`; `jobs/overdue-checker.ts` | `loan-report-new.tsx` | `loan_requests`, `loan_reports`; `gm_entries.is_loan` (separate) | No company co-pay, **no agreed return-date**, no loan-specific overdue/reminders; GM-loan flag & `loan_requests` **not unified** | Add return-date/co-pay, loan approval gate, overdue + reminders; unify GM-loan with loans module | No | 7 | Open |
| **P6** | P1 | Standardize post-approval invoice generation timing | `utils/invoice-utils.ts:23,36`; `gm-pool-routes.ts:468`; `hod-routes.ts:981`; `gm-bv-pool.repository.ts:308`; `account-routes.ts:2043` | `invoice-pool.tsx` | `product_posting_invoices` | 3 default invoices generated **at GM creation**, not after final approval | Move generation to a single point per confirmed timing; idempotent | **Yes** (Q4) | 8 | Open |
| **P7** | P0 | Validate manual invoice creator role + invoice type | `routes/invoice-routes.ts:50` (new, has `requireRole`); `account-routes.ts:1052` (legacy `POST /api/account/invoices`, auth-only); `services/invoice-workflow.service.ts` | `sales/create-invoice.tsx` (posts to legacy route; `canCreateInvoice==='sales_executive'`) | `product_posting_invoices`; `drm.invoices`; `serviceType`/`project_name` free-text | New `/api/invoices` enforces `requireRole`, but the **visible page posts to legacy `/api/account/invoices` (auth-only)**; invoice type is **free-text** (not enum) | Add invoice-type enum (Listing/Minisite/Product Posting); route manual creation through the role-guarded workflow OR add backend role guard to the legacy route | **Yes** (Q2) | 3 | Open |
| **P8** | P0 | Strengthen HOD & Accounts invoice approval validation | `account-routes.ts:1110` (legacy PATCH status), `:1075`, `:1140`; `services/invoice-workflow.service.ts` (new) | `invoice-pool.tsx`, HOD/Account dashboards | `drm.invoices` (legacy) vs `product_posting_invoices` (new) | Legacy `PATCH /api/account/invoices/:id/status` has **only `if(!req.user)`** — no role gate, no state machine, no audit (APR-001/INV-001 still open) | Retire/guard legacy route; enforce state machine + role + audit on all invoice tables | No | 5 | Open |
| **P9** | P0 | Standardize approved-invoice → project generation | `account-routes.ts:554` (`create-project-from-gm`, raw SQL, status `Active` L629); `repositories/projects.repository.ts:353`; `services/cross-department-status.service.ts` | account/PMS pages | `projects`, `project_*`, `product_posting_invoices` | Auto vs manual paths diverge; manual path bypasses `InvoiceWorkflowService`; initial status hardcoded `Active` | One canonical invoice→project path through the workflow service; confirm initial status | **Yes** (Q7) | 8 | Open |
| **P10** | P0 | Enforce Product Posting dependency on Listing-Page QA | `routes/product-posting-workflow-routes.ts` (assign-task); `services/product-posting-workflow.service.ts` | product-posting dashboards | `product_posting_workflows` (`qa_*`, `current_phase`), `projects` | **No backend block**: Product Posting can start without Listing-Page QA approval | Gate assign-task on Listing-Page QA-complete for same customer/order | **Yes** (Q5) | 9 | Open |
| **P11** | P1 | Align department lifecycle final status with Verification Manager stage | `routes/product-posting-workflow-routes.ts`; report/dashboard routes | QA/Verification dashboards | `product_posting_workflows.current_phase`; `PRODUCT_POSTING_PHASE_LABELS` | Verification stages exist & agree **for product posting only**; not uniformly required across all dept lifecycles | Define canonical final status incl. Verification across departments; align reports/UI | **Yes** (Q6) | 10 | Open |
| **P12** | P1 | Complete Accounts dashboard GM-type + invoice-status view | `dashboard-routes.ts:261-375`; `account-routes.ts` (gm-stats `loan_count`/`partial_payment_count`) | `account-manager-dashboard.tsx` | `gm_entries`, `product_posting_invoices`, `loan_requests` | Loan/partial **counts** present; missing financial **amounts/balances**, partial received vs pending, receipt history, loan return/overdue, invoice status per GM | Add GM-type financial breakdown + per-GM invoice status + loan/partial balance & receipt views | No | 11 | Open |
| **P13** | P1 | Clarify & align Service Executive GM / invoice participation | `service-core-routes.ts:464` (501 stub); `gm-pool-routes.ts`, `account-routes.ts`; `settings.middleware.ts` | `sales/create-invoice.tsx`, `app-sidebar.tsx` | `gm_entries`; `drm.url_permissions` | Service-Exec GM endpoint stubbed 501, invoice gated on frontend only, but generic GM routes fail open ⇒ backend does not actually block Service Exec | Decide & enforce Service-Exec scope in backend consistently | **Yes** (Q1, Q2) | 3 | Open |
| **P14** | P0 | Centralized cross-module workflow & status synchronization | `services/cross-department-status.service.ts`; bypassed by `account-routes.ts:1110,554` | (n/a) | `drm.cross_department_status_history`; GM/invoice/project/PP tables | Central service exists & audits, but legacy invoice-status PATCH and raw project creation **bypass** it | Route all GM/invoice/project/QA/verification status changes through the central service | No | 12 | Open |
