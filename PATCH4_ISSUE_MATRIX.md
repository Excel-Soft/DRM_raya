# PATCH 4 — Issue Matrix

Audit date: 2026-06-16. Stage 0 (audit only) — `Status` reflects current state,
not work done. Line numbers indicative as of audit date.

| Issue ID | Priority | Module | Route / Page | Frontend file | Backend file / API | DB table / source | Current gap | Required fix | Stage | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| ISS-01 | P0 | Domain Hosting — Server Names | `/it/servers` | `client/src/pages/it-servers.tsx` | `server/it-assets-routes.ts` (`/api/it/servers`, `/registries`, `/hosting-packages`, `/domains`); repo `server/repositories/it-assets.repository.ts` | `drm.it_servers`, `drm.it_registries`, `drm.it_hosting_packages`, `drm.it_domains`, `drm.it_backups` (all present) | Submit button is a no-op (no handler, no create mutation); form inputs uncontrolled; no validation/duplicate check; no error/empty states; backend GET/POST lack try/catch; `ensureItSchema()` creates unqualified tables vs `drm.*`; no in-page role guard; no audit | Add controlled state + create mutations, wire Submit, add field validation + duplicate guard + loading/error/empty states, wrap handlers in try/catch, fix schema qualification, add role guard + audit logging | Stage 1–2 | Open |
| ISS-01b | P0 | Domain Hosting — Domains | `/it/domains` | `client/src/pages/it-domains.tsx` | same `/api/it/domains` | `drm.it_domains` | Secondary surface; re-verify safe rendering of domain list + actions | Re-audit + apply same hardening as ISS-01 | Stage 1 | Open |
| ISS-02 | P2 | Support | `/support/tickets`, `/support/tickets/:id`, `/support/complaints`, `/service/complaint-list` | `client/src/pages/support-tickets.tsx`, `support-ticket-detail.tsx`, `complaints.tsx`, `service-complaint-list.tsx` | `server/support-routes.ts` (`/api/support/*`), `server/service-core-routes.ts` (`/api/service/complaints`) | `drm.support_tickets`, `drm.support_messages`, `drm.support_channel_config`, `drm.service_complaints` | Module not required this phase but visible in sidebar + reachable by direct URL | Hide sidebar "Support" entry; guard/disable `/support/*` (+ complaint routes) to block direct URL; keep code + tables for future phase; update menu/permission config | Stage 1 | Open |
| ISS-03 | P1 | Social Media Posting | `/social-media` (mock), `/drm/today-post`, `/drm/all-social-accounts` | `client/src/pages/social-media.tsx`, `client/src/pages/drm/today-post.tsx`, `client/src/pages/drm/all-social-accounts.tsx` | `server/today-post-routes.ts` (`/api/drm/today-posts`), `server/social-accounts-routes.ts` (`/api/drm/social-accounts`); mocks: `server/fb-routes.ts`, `server/bot-routes.ts` | `drm.today_posts`, `drm.social_accounts`, `drm.product_posting_data` (today_posts/social_accounts via runtime DDL, not in `shared/schema.ts`) | Sidebar points to mock `/social-media` (local state, no persistence); real CRUD lives on `/drm/*`; no schedule/publish execution; weak validation; no automated status verification | Repoint sidebar to real pages, add platform/account/content/media/schedule/status validation + tracking; define scope for actual schedule/publish | Stage 1–2 | Open |
| ISS-04 | P0 | Office Accounts (all sub-modules) | `/account/*`, `/office/*`, `/reports*` | see `OFFICE_ACCOUNTS_SUBMODULE_INVENTORY.md` | `server/office-account-routes.ts`, `server/account-routes.ts`, `server/reports-routes.ts` | `drm.account_heads`, `office_expenses`, `ledger_entries`, `donations`, `dollar_buying`/`dollar_buyers`, `cheques`, `invoices`, `business_customers`, `gm_entries`, `refund_gm_entries`; **`vas_documents` not found** | Two pages static/mock (Old Account Head, Trial Balance); Business Customers source divergence; financial POSTs validate types but not business rules; permission/audit coverage inconsistent; VAS documents source unconfirmed | Per-sub-module: wire static pages, confirm CRUD/report/export, add missing validation/permission/audit, harden financial writes, verify VAS source (no workflow change without sign-off) | Stage 1–2 | Open |
| SEC-01 | — (out of Patch 4 scope) | Auth config | n/a | n/a | `.replit` | n/a | Hardcoded `JWT_SECRET` in `.replit` (pre-existing) | Move to Replit Secrets + rotate | Separate | Noted |
